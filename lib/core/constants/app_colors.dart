import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary Brand Colors
  static const Color primary = Color(0xFF0F172A); // Slate-900
  static const Color secondary = Color(
    0xFF065F46,
  ); // Emerald Gelap (Pembaruan Terbaru)

  // Neutral Colors (Background & Surfaces)
  static const Color background = Color(0xFFF8F9FF); // Surface
  static const Color surfaceContainer = Color(0xFFEFF4FF); // Container-low

  // Typography Colors
  static const Color textPrimary = Color(0xFF0F172A); // On-surface
  static const Color textSecondary = Color(0xFF64748B); // Slate-500

  // Semantic Colors
  static const Color error = Color(0xFFB00020);
  static const Color success = Color(
    0xFF065F46,
  ); // Menggunakan emerald gelap untuk keberhasilan

  // Additional Accents from Design System
  static const Color outline = Color(0xFFCBDBF5); // Border & Outline-variant
}
